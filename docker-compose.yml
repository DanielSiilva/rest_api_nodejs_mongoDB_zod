version: "3.1"

services:
  api-rest-node:
    image: mongo
    container_name: rest-api-nodejs-mongodb
    ports:
      - 27017:27017
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
